# 🐧 리눅스 서버 구축 시험 총정리

> 💡 Ubuntu에서 SSH → FTP → Apache → MariaDB 순서로 서버를 구축하고, Windows(PuTTY / FileZilla / Chrome / HeidiSQL)로 접속 확인하는 것이 핵심

---

## 🔀 버전 선택 — 내 상황에 맞는 것 고르기

| 버전 | 상황 | SSH 설치 |
|------|------|----------|
| **버전 1** | 새 가상머신 — 처음부터 세팅 | 직접 설치 필요 |
| **버전 2** | 기존 가상머신 — PuTTY 접속 이미 됨 | 생략 가능 |

> 💡 **버전 2라면**: SSH 섹션(01)은 `ip addr`로 IP 확인 + `sudo systemctl status ssh`로 active 확인만 하고 넘어가면 됨. 나머지 단계(FTP부터)는 동일.

---

## 📋 전체 흐름 (공통)

| 단계 | 작업 |
|------|------|
| ① | `sudo apt install` 로 패키지 설치 |
| ② | `sudo vi [설정파일]` 로 설정 수정 |
| ③ | `sudo systemctl restart [서비스명]` 재시작 |
| ④ | `sudo systemctl status [서비스명]` → active 확인 |
| ⑤ | Windows 클라이언트에서 접속 테스트 |

> 💡 **vi 사용법**: `i` → 편집모드 → 수정 → `ESC` → `:wq` (저장 후 종료) / `:q!` (저장 없이 종료) / status 화면 탈출: `q`

---

## 🟡 00. 사전 준비 (제일 먼저 실행)

```bash
sudo apt update          # 설치 목록 최신화
sudo apt install vim     # 편집기 설치
```

---

## 🟢 01. SSH 서버 설정 (포트 22)

> SSH = Windows(PuTTY)에서 Ubuntu에 **원격 접속**하게 해주는 서비스

### 명령어 순서 (Ubuntu 터미널)

| # | 명령어 | 설명 |
|---|--------|------|
| 1 | `sudo apt install openssh-server` | SSH 서버 설치 |
| 2 | `sudo vi /etc/ssh/sshd_config` | 설정 파일 열기 |
| 3 | `sudo systemctl restart ssh` | 설정 적용 |
| 4 | `sudo systemctl status ssh` | active 확인 → `q` 나가기 |
| 5 | `ip addr` | 내 IP 확인 (192.168.xxx.xxx) |

### 설정 파일 수정 — `/etc/ssh/sshd_config`

```
#Port 22  →  Port 22          (# 제거하여 활성화)
ListenAddress 0.0.0.0         (모든 IP에서 접속 허용)
```

| 설정값 | 의미 |
|--------|------|
| `Port 22` | 22번 포트로 접속 |
| `0.0.0.0` | 모든 IP에서 접속 허용 |

> ⚠️ **버전 2 (기존 PuTTY 접속 가능한 경우)**: `ip addr`로 IP 확인 + `status ssh`로 active 확인만 하면 됨. 설치 과정 생략 가능.

---

## 🔵 02. PuTTY 접속 (Windows)

| 항목 | 입력값 |
|------|--------|
| Host Name | Ubuntu IP (192.168.xxx.xxx) |
| Port | 22 |
| Connection type | SSH |

`Open` → `login as:` Ubuntu 아이디 → `password:` Ubuntu 비밀번호

### 아이디/비밀번호 모를 때

```bash
whoami                         # 현재 아이디 확인
sudo passwd 사용자아이디       # 비밀번호 재설정 (예: sudo passwd ubuntu)
```

> ✅ 접속 성공하면 이후 모든 작업은 PuTTY에서 진행

---

## 🔵 03. FTP 서버 설정 (포트 21)

> FTP = Windows(FileZilla)와 Ubuntu 사이에서 **파일을 주고받는** 서비스

### 명령어 순서 (PuTTY에서)

| # | 명령어 | 설명 |
|---|--------|------|
| 1 | `sudo apt install vsftpd` | FTP 서버 설치 |
| 2 | `sudo vi /etc/vsftpd.conf` | 설정 파일 열기 |
| 3 | `sudo systemctl restart vsftpd` | 재시작 |
| 4 | `sudo systemctl status vsftpd` | active 확인 → `q` 나가기 |

### 설정 파일 — `/etc/vsftpd.conf`

> 앞에 `#` 있으면 제거해서 활성화

```
listen=YES          ← 필수
listen_ipv6=NO      ← 필수 (충돌 방지)
anonymous_enable=NO ← 익명 접속 차단
local_enable=YES    ← 필수
write_enable=YES    ← 필수
```

| 설정값 | 의미 | 중요도 |
|--------|------|--------|
| `listen=YES` | FTP 서버 켜기 (IPv4) | 필수 |
| `listen_ipv6=NO` | IPv6 끄기 — listen=YES와 충돌 방지 | 필수 |
| `anonymous_enable=NO` | 익명(아이디 없이) 접속 차단 | 권장 |
| `local_enable=YES` | Ubuntu 계정으로 로그인 허용 | 필수 |
| `write_enable=YES` | 파일 업로드(쓰기) 허용 | 필수 |

> ⚠️ **listen=YES면 listen_ipv6=NO 반드시 같이.** 안 하면 서비스 실행 안 됨.

---

## 🔵 04. FileZilla 접속 (Windows)

| 항목 | 입력값 |
|------|--------|
| 호스트 | Ubuntu IP |
| 사용자명 | Ubuntu 로그인 아이디 |
| 비밀번호 | Ubuntu 로그인 비밀번호 |
| 포트 | 21 |

"빠른 연결" 클릭 → 파일 업로드/다운로드 테스트

---

## 🟠 05. Apache 웹 서버 (포트 80)

> Apache = 브라우저(Chrome)에서 **웹페이지를 볼 수 있게** 해주는 서비스. 설치하면 바로 동작.

| # | 명령어 | 설명 |
|---|--------|------|
| 1 | `sudo apt install apache2` | Apache 설치 (자동 실행) |
| 2 | `sudo systemctl status apache2` | active 확인 → `q` 나가기 |
| 3 | Chrome → `http://[Ubuntu IP]` | 기본 페이지 보이면 성공 |

> ✅ **4개 서버 중 유일하게 설정 파일 수정 없이 바로 동작**

---

## 🟠 06. index.html 만들어서 업로드

### 1단계 — Windows 메모장에서 작성

```html
<html>
  <head><title>학번 이름</title></head>
  <body><h1>학번 이름</h1></body>
</html>
```

### 2단계 — 저장 시 주의

| 항목 | 설정 |
|------|------|
| 파일 이름 | `index.html` |
| 파일 형식 | **모든 파일** 선택 |

> ⚠️ "모든 파일" 안 하면 `index.html.txt`로 저장됨 → 웹에서 안 열림

### 3단계 — Ubuntu에 업로드 (방법 2가지)

**방법 A** — FileZilla에서 `/var/www/html/`에 직접 업로드
- 오른쪽 원격 경로를 `/var/www/html/`로 변경 → 드래그앤드롭
- 권한 오류 시: `sudo chmod 777 /var/www/html`

**방법 B** — 홈 폴더에 올린 후 PuTTY에서 복사 (추천)
```bash
sudo cp index.html /var/www/html/index.html
# 파일 이름이 다르면:
sudo cp html.html /var/www/html/index.html
```

### 4단계 — 확인

- Chrome → `http://[Ubuntu IP]` → 내가 만든 페이지(학번+이름)가 보이면 성공
- **최종 파일 위치**: `/var/www/html/index.html`

---

## 🟣 07. MariaDB 설치 및 DB 생성 (포트 3306)

> MariaDB = 데이터를 저장·관리하는 창고. MySQL과 거의 동일.

### 1단계 — 설치

| # | 명령어 | 설명 |
|---|--------|------|
| 1 | `sudo add-apt-repository universe` | universe 저장소 추가 |
| 2 | `sudo apt update` | 저장소 목록 갱신 |
| 3 | `sudo apt install mariadb-server` | MariaDB 설치 |
| 4 | `sudo systemctl start mariadb` | MariaDB 실행 |
| 5 | `sudo systemctl status mariadb` | active 확인 → `q` |

> 🔧 `add-apt-repository` 명령어 없으면: `sudo apt install software-properties-common` 먼저 실행

### 2단계 — 보안 설정

```bash
sudo mysql_secure_installation
```

> 💡 첫 번째 질문 "Enter current password for root (enter for none):" → **아무것도 입력 안 하고 Enter**

### 3단계 — DB 접속

```bash
mariadb -u root -p     # 접속 안 되면:
sudo mariadb           # 이걸로 대신 접속
```

### 4단계 — DB 내부 명령어 (순서대로 실행)

```sql
use mysql;                                          -- 1. mysql 시스템 DB 선택 (첫 번째로 반드시)

create database aiswtest;                           -- 2. DB(창고) 만들기

create user 'aiswdb'@'%' identified by '@Polytech'; -- 3. 사용자 만들기

grant all privileges on aiswtest.* to 'aiswdb'@'%' with grant option; -- 4. 권한 부여

flush privileges;                                   -- 5. 권한 즉시 적용

exit;                                               -- 6. 나가기
```

| 명령어 | 뜻 |
|--------|----|
| `use mysql;` | mysql 시스템 DB 선택 (접속 후 첫 번째 실행) |
| `create database` | 데이터 창고(DB) 만들기 |
| `'aiswdb'@'%'` | 어디서든(%) 접속 가능한 사용자 aiswdb |
| `identified by` | 비밀번호 지정 |
| `grant all privileges` | 모든 권한 주기 |
| `flush privileges` | 권한 즉시 반영 |

> 🔧 **create user에서 "이미 존재" 에러 뜨면**: `create user` 생략하고 아래만 실행
> ```sql
> grant all privileges on aiswtest.* to 'aiswdb'@'%' with grant option;
> flush privileges;
> exit;
> ```

### 5단계 — 외부 접속 설정

```bash
sudo vi /etc/mysql/mariadb.conf.d/50-server.cnf
```

```
bind-address = 127.0.0.1  →  bind-address = 0.0.0.0
```

| 값 | 의미 |
|----|------|
| `127.0.0.1` | 자기 자신(로컬)에서만 접속 |
| `0.0.0.0` | 모든 IP에서 접속 허용 → HeidiSQL 접속 가능 |

```bash
sudo systemctl restart mariadb   # 설정 적용
sudo systemctl status mariadb    # active 확인
```

> ⚠️ restart 시 `mysqld` 아님. Ubuntu에서는 반드시 `mariadb`

---

## 🟣 08. HeidiSQL 접속 (Windows)

| 항목 | 입력값 |
|------|--------|
| 네트워크 유형 | MariaDB or MySQL |
| 호스트명/IP | Ubuntu IP (192.168.xxx.xxx) |
| 사용자 | aiswdb |
| 암호 | @Polytech |
| 포트 | 3306 |

"열기" 클릭 → 왼쪽에 **aiswtest**와 **information_schema** 보이면 성공

---

## 📊 09. 4개 서버 한눈에 비교

| 서버 | 패키지 | 설정 파일 | 재시작 명령어 | 포트 | Windows 도구 |
|------|--------|-----------|---------------|------|-------------|
| SSH | openssh-server | /etc/ssh/sshd_config | restart ssh | 22 | PuTTY |
| FTP | vsftpd | /etc/vsftpd.conf | restart vsftpd | 21 | FileZilla |
| Apache | apache2 | 수정 없음 | restart apache2 | 80 | Chrome |
| MariaDB | mariadb-server | 50-server.cnf | restart mariadb | 3306 | HeidiSQL |

### 최종 성공 기준

| 도구 | 확인 방법 |
|------|----------|
| 🖥️ PuTTY | SSH 접속 성공 |
| 📁 FileZilla | 파일 업/다운 성공 |
| 🌐 Chrome | 학번+이름 웹페이지 확인 |
| 🗄️ HeidiSQL | aiswtest DB 확인 |

---

## ⚠️ 10. 자주 틀리는 실수 + 트러블슈팅

### 자주 틀리는 실수

| ❌ 실수 | ✅ 올바른 것 |
|---------|-------------|
| `systemctl restart mysqld` | `systemctl restart mariadb` |
| `listen=YES`만 설정 | `listen=YES` + `listen_ipv6=NO` 같이 |
| `flush privileges` 빠뜨리기 | grant 후 반드시 `flush privileges;` |
| `use mysql;` 빠뜨리기 | mariadb 접속 후 첫 명령어로 실행 |
| index.html 저장 시 파일 형식 미변경 | 파일 형식 "모든 파일" 선택 |
| `#Port 22` 그대로 (주석 미제거) | `#` 제거 → `Port 22` 활성화 |
| `bind-address = 127.0.0.1` 그대로 | `0.0.0.0`으로 수정해야 외부 접속 가능 |

### 트러블슈팅

| 문제 | 해결 |
|------|------|
| `mariadb -u root -p` 접속 안 됨 | `sudo mariadb`로 접속 |
| create user 에러 (이미 존재) | create user 생략 → grant + flush만 실행 |
| add-apt-repository 명령어 없음 | `sudo apt install software-properties-common` |
| MariaDB 패키지 못 찾음 | `sudo add-apt-repository universe` 후 apt update |
| FileZilla 업로드 권한 거부 | 홈 폴더에 올린 후 `sudo cp` 복사 |
| 아이디 모르겠음 | `whoami` |
| 비밀번호 모르겠음 | `sudo passwd 사용자아이디` |
| status 화면에서 못 나감 | `q` 키 누르면 됨 |

---

## 📝 11. 시험 예상 문제 15선

| # | 질문 | 정답 |
|---|------|------|
| Q01 | 서버 설치 전 제일 먼저 실행하는 명령어 2개? | `sudo apt update` → `sudo apt install vim` |
| Q02 | `#Port 22`에서 `#`의 의미와 활성화 방법? | `#` = 주석(비활성화). `#` 제거 → `Port 22`로 활성화 |
| Q03 | Ubuntu 아이디 확인 명령어와 비밀번호 재설정 명령어? | 아이디: `whoami` / 비밀번호: `sudo passwd 사용자아이디` |
| Q04 | FTP 설정에서 필수 3가지 + 충돌 방지 1가지? | 필수: `listen=YES`, `local_enable=YES`, `write_enable=YES` / 충돌 방지: `listen_ipv6=NO` |
| Q05 | `anonymous_enable=NO`의 의미? | 익명(아이디 없이) 접속 차단. Ubuntu 계정으로만 로그인 가능 |
| Q06 | 메모장에서 index.html 저장 시 주의할 점? | 파일 형식을 "모든 파일"로 선택. 안 하면 `index.html.txt`로 저장됨 |
| Q07 | FileZilla로 직접 업로드 안 될 때 대안은? | 홈 폴더에 올린 후 PuTTY에서: `sudo cp index.html /var/www/html/index.html` |
| Q08 | MariaDB 패키지가 안 잡힐 때 해결 방법? | `sudo add-apt-repository universe` → `sudo apt update` |
| Q09 | mysql_secure_installation 첫 질문 답은? | 아무것도 안 치고 Enter |
| Q10 | `mariadb -u root -p` 접속 안 될 때? | `sudo mariadb`로 접속 |
| Q11 | mariadb 접속 후 첫 번째 명령어? | `use mysql;` |
| Q12 | bind-address 127.0.0.1과 0.0.0.0의 차이? | 127.0.0.1 = 자기 자신만 / 0.0.0.0 = 모든 IP 허용 → HeidiSQL 접속 가능 |
| Q13 | create user 에러(이미 존재) 시 대처 방법? | create user 건너뛰고: `grant all privileges on aiswtest.* to 'aiswdb'@'%' with grant option; flush privileges;` |
| Q14 | 4개 서버 포트 번호? | SSH 22 · FTP 21 · Apache 80 · MariaDB 3306 |
| Q15 | HeidiSQL 접속 성공 기준? | 왼쪽에 **aiswtest**와 **information_schema** 보이면 성공 |

---

📅 업데이트: 2026-06-11
